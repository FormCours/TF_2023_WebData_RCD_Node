const authController = {
    /**
     * Register
     * @param { Request } req
     * @param { Response } res
     */
    register : async(req, res) => {

    },

    /**
     * Login
     * @param { Request } req
     * @param { Response } res
     */
    login : async(req, res) => {

    }
}

module.exports = authController